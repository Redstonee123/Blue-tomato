# Blue Tomato Live Server

## Setup
1) Copy `.env.example` to `.env` and fill values.
2) Install deps: `npm install`
3) Start: `npm run dev`

## Endpoints
- POST /api/live/start
- POST /api/live/stop
- GET /api/live/list
- GET /api/live/urls?streamName=xxx
